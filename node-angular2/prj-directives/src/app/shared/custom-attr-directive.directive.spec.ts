import { CustomAttrDirectiveDirective } from './custom-attr-directive.directive';

describe('CustomAttrDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new CustomAttrDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
