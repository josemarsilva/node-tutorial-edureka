import { HighlightCompleteDirective } from './highlight-complete.directive';

describe('HighlightCompleteDirective', () => {
  it('should create an instance', () => {
    const directive = new HighlightCompleteDirective();
    expect(directive).toBeTruthy();
  });
});
