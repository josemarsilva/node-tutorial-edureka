import { HighlightMouseDirectiveDirective } from './highlight-mouse-directive.directive';

describe('HighlightMouseDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new HighlightMouseDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
